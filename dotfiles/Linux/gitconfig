[user]
        name = Sergei Belokon
        email = srj.belokon@gmail.com
        signingkey = 9602C3B5B79665D7
[alias]
        s = status -sb
        l = log --pretty=format:'%C(white)%ae %ar%C(reset)%n%C(bold)%h%C(reset) %s%C(auto)%d%n%+b'
        b = branch
        r = remote update origin --prune ; update the local list of remote branches
[color]
        ui = auto
[color "branch"]
        current = cyan
[core]
        editor = vim
[push]
        default = simple
[github]
        user = enkron
[init]
        defaultBranch = main
