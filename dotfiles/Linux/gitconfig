[user]
        name = Sergei Belokon
        email = srj.belokon@gmail.com
[alias]
        s = status -sb
        l = log --pretty=format:'%C(blue)%h%C(reset) %ae %ar%C(auto)%d%n    %C(yellow)%s%C(reset)%n%b'
        b = branch
        r = remote update origin --prune ; update the local list of remote branches
[color]
        ui = auto
[color "branch"]
        current = cyan
[core]
        editor = vim
[push]
        default = simple
[github]
        user = enkron
[init]
        defaultBranch = main
